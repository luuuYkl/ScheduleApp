# 更新日志 / Changelog

## [2026-01-11] - UI 优化与功能增强

### ✨ 新增功能

#### 1. AI 功能优化
- **日志页面 AI 分析优化**
  - 移除手动生成和刷新按钮，保留 AI 智能分析
  - 新增任务预览面板，显示 AI 将分析的任务列表
  - 修复本地时区问题，确保"今天/本周/本月"筛选准确
  - 添加任务加载逻辑，解决 AI 无法读取任务数据的问题
  - 优化错误提示，显示筛选的任务数量和日期范围

- **计划创建页面优化**
  - 修复 AI 建议任务添加按钮会误触发表单提交的问题
  - 所有 AISuggestions 组件按钮添加 `type="button"` 防止表单误提交
  - 优化任务添加逻辑，添加重复检测和用户提示
  - 更新待创建任务列表提示文本，明确需要点击"保存计划"才会创建

#### 2. 主题系统优化
- **明亮模式修复**
  - 修复明亮模式下卡片 hover 时变黑的问题
  - 统一使用主题变量 `--bg-card-hover` 和 `--shadow-sm`
  - 移除硬编码的深色背景和白色阴影

#### 3. 日程创建功能重构
- **新的日程创建表单**
  - 采用纵向卡片式布局，事件型思维设计
  - 添加快捷日期选择（今天/明天/下周）
  - 新增提醒方式选项（事件开始时/提前10分钟/提前1小时/不提醒）
  - 添加全天事件切换功能
  - 新增地点输入字段
  - 新增重复规则下拉选择（不重复/每天/每周/每月）
  - 备注字段限制100字符，显示字数统计
  - 更多选项可折叠展开，保持界面轻快

### 🎨 界面优化

#### 1. 任务列表统一样式
- **5层信息结构标准化**
  - 两列网格布局：56px 时间列 + 内容列
  - 第一层：时间/日期（12px，tabular-nums，弱色）
  - 第二层：勾选框 + 状态圆点（6px）+ 标题（14px，font-weight 500）
  - 第三层：副信息（12px，时长/备注等）
  - 第四层：标签（11px，圆角，语义色彩）
  - 第五层：操作按钮（轻量化）

- **应用范围**
  - ✅ 首页今日任务与日程列表
  - ✅ 计划页面任务列表
  - 统一视觉语言，提升一致性

#### 2. 任务详情页优化
- **3段式垂直布局**
  - 标题区：状态点 + 24px 标题 + 属性栏（日期/时间/时长/重复）
  - 内容区：进度条 + 勾选框 + 描述
  - AI 分析区：可折叠面板，左侧彩色边框，智能建议

#### 3. 计划进度概览重构
- **响应式卡片布局**
  - 桌面端：2-3列网格，卡片等高
  - 移动端：横向滑动，每次露出1.2张卡片
  - 超过6个计划显示"查看全部"按钮

- **4要素卡片结构**
  - 🎯 计划名称（14-16px，最多两行）
  - 进度表达：█ █ ▒ ▒ ▒ 分段进度条 + 百分比
  - 当前状态：灰色非强调文本（初期积累/稳步推进/最近7天无推进）
  - 🤖 AI 洞察：可折叠，智能建议基于推进情况

- **智能排序**
  - 优先显示正在进行的计划
  - 其次按有推进的计划
  - 最后按创建时间倒序

#### 4. 页面布局修复
- **统一页面间距**
  - 所有页面添加 `padding-top: calc(var(--header-height, 64px) + 1rem)`
  - 避免内容被固定顶部导航栏遮挡
  - 修复页面：HomePage, SchedulePage, TaskDetailPage, PlanTasksPage, PlanEditPage, PlanCalendarPage

#### 5. 按钮样式优化
- **首页计划进度概览**
  - "管理任务"按钮样式统一为 `secondary` 类
  - 移除 hover 变色效果，保持一致性

- **首页清理**
  - 移除"查看日志"和"刷新数据"按钮区块
  - 界面更简洁，聚焦核心功能

### 🐛 Bug 修复

1. **日期时区问题**
   - 修复"今天"任务筛选因 UTC 偏移导致不匹配的问题
   - 添加 `toLocalDateString()` 和 `parseLocalDate()` 工具函数
   - 确保日期过滤使用本地时区

2. **表单提交问题**
   - 修复 AI 建议任务添加按钮误触发表单提交
   - 所有非提交按钮添加 `type="button"` 属性

3. **明亮模式兼容**
   - 修复卡片 hover 时在明亮模式下显示黑色的问题
   - 使用主题变量替代硬编码颜色值

### 🔧 技术改进

1. **样式系统优化**
   - 统一使用 CSS 变量 `--bg-card-hover`, `--shadow-sm` 等
   - 移除硬编码颜色值，提升主题兼容性
   - 标准化组件样式结构

2. **代码清理**
   - 删除不必要的 TypeScript 编译产物 (*.vue.js)
   - 优化组件导入和依赖关系
   - 移除未使用的函数和样式

3. **辅助函数增强**
   - `formatDate()`: 智能日期显示（今天/明天/昨天/MM-DD）
   - `calcDuration()`: 计算任务时长
   - `statusLabel()`: 状态文本格式化
   - `toLocalDateString()`: 本地时区日期字符串
   - `parseLocalDate()`: 解析日期字符串到本地 Date 对象

### 📝 文档更新

- 更新 README.md
- 新增多个 AI 功能文档
  - AI_COMPLETION_REPORT.md
  - AI_FEATURE_GUIDE.md
  - AI_FEATURE_IMPLEMENTATION.md
  - AI_INTEGRATION_SUMMARY.md
  - AI_REVIEW_SUMMARY.md
  - QUICK_START_AI.md

### 🎯 设计原则

本次更新遵循以下设计理念：

1. **计划不是进度追踪，而是方向确认**
   - 弱化具体百分比，强化阶段感和节奏感
   - 使用分段进度条替代传统进度条
   - AI 洞察聚焦在推进节奏而非完成度

2. **事件型思维 vs 任务型思维**
   - 日程创建强调时间和提醒
   - 任务管理强调完成状态和进度
   - 清晰区分两者的使用场景

3. **移动优先，响应式自适应**
   - 移动端横向滑动体验
   - 桌面端多列网格展示
   - 触摸友好的交互设计

4. **信息层级标准化**
   - 5层信息结构应用于所有列表
   - 统一视觉语言和间距系统
   - 提升整体一致性

---

## [Unreleased]

### 🐛 Bug Fixes
- 修复月度重复任务算法缺陷，正确处理月末日期计算 (#BUG-001)
  - 解决1月31日重复任务跳转到3月2日的问题
  - 实现"目标月份 + 原始 day 上限裁剪"策略
  - 确保不产生不存在的日期
  - 完善相关测试用例

- 修复Store方法返回值不一致问题 (#BUG-002)
  - 统一loadPlans()和loadTasks()的返回值行为
  - 解决Promise.all()并发调用拿到undefined的问题
  - 增强API一致性和并发安全性
  - 添加完整的并发调用测试覆盖

- 实现ID冲突主动处理策略 (#ARCH-001)
  - createPlan方法现在检查并处理ID冲突
  - 相同ID时更新现有计划而非新增
  - 提升数据一致性和系统可靠性
  - 添加全面的ID冲突处理测试
  - 详细记录设计决策和权衡分析

- 修复测试用例时间漂移问题 (#TEST-001)
  - 统一采用相对时间窗口策略
  - 消除固定日期导致的持续失败
  - 提高测试稳定性和可维护性
  - 添加专门的时间漂移修复测试
  - 创建测试辅助工具函数

- 增强Store层类型安全 (#TYPE-001)
  - plans/tasks状态使用明确的Plan[]/Task[]类型
  - createPlan参数使用Partial<Plan>类型
  - createTask参数使用CreateTaskPayload类型
  - 消除any类型的使用，提升TypeScript保护
  - 添加类型安全验证测试
  - 完善类型约束和编译时检查

- 修复plans-fixed.spec.ts类型错误 (#FIX-001)
  - 修正测试数据类型从Plan到CreateTaskPayload
  - 解决generateRepeatTaskPayloads参数类型不匹配问题
  - 统一测试数据结构与函数接口
  - 确保TypeScript类型检查通过

## 下一步计划

- [ ] 添加计划归档功能
- [ ] 优化 AI 分析算法
- [ ] 添加数据导出功能
- [ ] 支持自定义主题配色
- [ ] 添加团队协作功能
