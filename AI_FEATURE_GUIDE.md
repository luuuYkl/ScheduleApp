# AI 智能优化功能使用指南

## 功能概述

ScheduleApp 现已集成 AI 智能优化功能，可以帮助用户：
- 分析计划的合理性
- 提供时间安排建议
- 自动拆解任务清单
- 优化计划标题和描述

## 配置步骤

### 1. 获取 OpenAI API Key

访问 [OpenAI Platform](https://platform.openai.com/) 注册账号并获取 API Key。

### 2. 配置环境变量

复制 `.env.example` 为 `.env.local`：

```bash
cp .env.example .env.local
```

编辑 `.env.local` 文件，填入你的 API Key：

```env
VITE_OPENAI_API_KEY=sk-your-actual-api-key-here
VITE_OPENAI_BASE_URL=https://api.openai.com/v1
```

### 3. 重启开发服务器

```bash
npm run dev
```

## 使用方法

### 创建计划时使用 AI 优化

1. 进入"创建新计划"页面
2. 填写基本信息：
   - 标题（必填，至少2个字符）
   - 描述（可选，但填写后 AI 建议更精准）
   - 开始日期（必填）
   - 结束日期（必填）
3. 点击 **🤖 AI 智能优化** 按钮
4. 等待 AI 分析（通常3-5秒）

### AI 提供的建议类型

AI 会分析你的计划并提供以下建议：

#### 1. 警告（⚠️）
- 时间安排不合理
- 必填字段缺失
- 日期逻辑错误

#### 2. 建议（💡）
- 如何改进计划
- 更好的描述方式
- 时间分配优化

#### 3. 信息（ℹ️）
- 计划特征分析
- 预期效果说明

### 推荐任务清单

AI 会根据你的计划标题和描述，自动生成建议的任务列表：

1. 点击任务旁边的 **➕** 按钮添加到"待创建任务"
2. 你可以随时删除不需要的任务
3. 保存计划时，选中的任务会自动创建

### 应用优化建议

如果 AI 提供了优化后的标题、描述或日期：

1. 点击 **✨ 应用优化建议** 按钮
2. 表单会自动填充 AI 优化后的内容
3. 你可以继续手动调整

## 功能配置

### 禁用 AI 功能

如果不需要 AI 功能，在 `src/config.ts` 中设置：

```typescript
AI_ENABLED: false
```

### 使用其他 AI 服务

如果你使用兼容 OpenAI API 的其他服务（如 Azure OpenAI、国内镜像等）：

编辑 `.env.local`：

```env
VITE_OPENAI_API_KEY=your-api-key
VITE_OPENAI_BASE_URL=https://your-custom-endpoint.com/v1
```

### 更换 AI 模型

在 `src/config.ts` 中修改：

```typescript
AI_MODEL: "gpt-4o-mini" // 或 "gpt-4", "gpt-3.5-turbo" 等
```

## Mock 模式

如果未配置 API Key，系统会自动启用 Mock 模式：
- 基于规则生成本地建议
- 不调用真实 AI API
- 不产生费用
- 建议质量较基础但仍有参考价值

## 注意事项

### API 费用
- AI 优化功能会调用 OpenAI API，产生少量费用
- 每次优化约消耗 200-500 tokens（约 $0.001-0.005）
- 建议设置 OpenAI 账户消费限额

### 隐私
- 你的计划数据会发送到 OpenAI 服务器进行分析
- 如果介意隐私，可以禁用 AI 功能或使用自部署的 AI 服务

### 网络要求
- 需要能访问 OpenAI API
- 中国大陆用户可能需要配置代理或使用国内镜像服务

## 故障排查

### AI 优化按钮灰色不可点击
- 检查是否填写了标题和日期
- 标题至少需要2个字符

### 点击后没有响应
- 打开浏览器控制台查看错误信息
- 检查 API Key 是否正确配置
- 确认网络连接正常

### 总是显示 Mock 建议
- 检查 `.env.local` 文件是否存在
- 确认 API Key 格式正确（以 `sk-` 开头）
- 重启开发服务器

### API 调用失败
- 检查 OpenAI 账户余额
- 验证 API Key 权限
- 确认 BASE_URL 配置正确

## 示例

### 输入示例
```
标题: 每日晨跑
描述: 培养早起运动的习惯，提升身体素质
开始日期: 2026-01-15
结束日期: 2026-02-14
```

### AI 建议示例
- ℹ️ 计划时长为 31 天，适合培养习惯或完成中短期目标
- 💡 建议添加详细描述，说明计划的目标、预期成果和执行方式
- 📋 推荐任务：
  - 制定每周训练计划
  - 记录每次锻炼的时长和强度
  - 监测体重和体脂变化
  - 调整饮食计划配合训练

## 反馈与改进

如果你有功能建议或发现问题，欢迎：
- 提交 Issue
- 参与讨论
- 贡献代码

---

享受智能化的计划管理体验！🚀
